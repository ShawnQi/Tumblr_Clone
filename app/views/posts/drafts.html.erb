<h3>Drafts</h3>

<% if @drafts %>
	<ul>
	<% @drafts.each do |draft| %>
		<li>
			Title: <%= draft.title %><br>
			Body: <%= draft.body %><br>
			<a href="<%= publish_url(draft.id) %>">Publish</a>
		</li>
	<% end %>
	</ul>
<% end %>